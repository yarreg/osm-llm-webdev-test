You previously generated three files for a web page. However, there are some errors. Please fix them and call the emit_files tool with the full corrected files.

{% if call_method == "json" %}
Return exactly one JSON object with keys index_html, app_js, styles_css (values are full file contents). No other text.
{% endif %}

{% if call_method == "tools" %}
Call the emit_files tool with the full content of the three files.
{% endif %}

{% if console_messages %}
--- CONSOLE MESSAGES ---
{{ console_messages }}
{% endif %}

{% if not missing_required_sections %}
--- FAILING CHECKS ---
{% for section in missing_required_sections %}
- {{ section }}
{% endfor %}
{% endif %}

{#

--- CURRENT FILES ---

-- index.html --
{{ generated_files['index.html'] or '' }}

-- app.js --
{{ generated_files['app.js'] or '' }}

-- styles.css --
{{ generated_files['styles.css'] or '' }}

#}